name: Publish documentation

on:
  workflow_call:
    inputs:
      tooling_revision:
        required: true
        type: string
      source_sha:
        required: true
        type: string

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-24.04
    concurrency:
      group: documentation-publication
      cancel-in-progress: false
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ inputs.source_sha }}
          fetch-depth: 0
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          repository: hubuum/.github
          ref: ${{ inputs.tooling_revision }}
          path: target/docs-tooling
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: '3.12'
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: documentation-site
          path: target/docs-preview
      - name: Retain the published archive
        shell: bash
        run: |
          if git ls-remote --exit-code --heads origin gh-pages; then
            git fetch origin gh-pages
            git worktree add --detach target/docs-published FETCH_HEAD
          else
            git worktree add --detach target/docs-published HEAD
            git -C target/docs-published switch --orphan gh-pages
          fi
          python3 target/docs-tooling/docs-tooling/publish.py
          python3 target/docs-tooling/docs-tooling/check-docs.py --site-dir target/docs-published/site --skip-navigation
          git -C target/docs-published add site
          if ! git -C target/docs-published diff --cached --quiet; then
            git -C target/docs-published -c user.name=github-actions[bot] \
              -c user.email=41898282+github-actions[bot]@users.noreply.github.com \
              commit -m 'Publish documentation'
            git -C target/docs-published push origin HEAD:refs/heads/gh-pages
          fi
      - uses: actions/upload-pages-artifact@7b1f4a764d45c48632c6b24a0339c27f5614fb0b # v4
        with:
          path: target/docs-published/site/
      - uses: actions/configure-pages@983d7736d9b0ae728b81ab479565c72886d7745b # v5
      - id: deployment
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e # v4
